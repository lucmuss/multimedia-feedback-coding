# =============================================================================
# API Keys - Diese werden beim Start automatisch geladen
# =============================================================================
# Kopieren Sie diese Datei nach .env und fügen Sie Ihre echten API-Keys ein.
# Die .env Datei wird NIEMALS in Git committed (sie ist in .gitignore).
#
# Wichtig: API-Keys werden NICHT in settings.json gespeichert!
# settings.json enthält nur Platzhalter ("USE_ENV_FILE").

# OpenAI API Key - Für Transkription (gpt-4o-transcribe)
OPENAI_API_KEY=your-openai-api-key-here

# Replicate API Token - Für lokale Modelle (Llama, Qwen)
REPLICATE_API_KEY=your-replicate-api-token-here

# OpenRouter API Key - Alternative zu Replicate
OPENROUTER_API_KEY=your-openrouter-api-key-here

# =============================================================================
# Debugging & Logging
# =============================================================================
# Setzen Sie DEBUG=true für detaillierte Logs (auch in Dateien)
DEBUG=true
LOG_LEVEL=DEBUG

# Optional: Live-Capture deaktivieren (für Headless-Systeme)
# DISABLE_LIVE_CAPTURE=true